"""
Fix for Python 3.13.5 readline error on Windows
This fixes the interactive Python shell but is NOT needed for the trading bot
"""

import os
import sys

# Set this as PYTHONSTARTUP environment variable
os.environ['PYTHONSTARTUP'] = ''

# Or install pyreadline3 for better experience:
# pip install pyreadline3

print("To fix the readline error permanently:")
print("1. Run: pip install pyreadline3")
print("2. Or use IPython instead: pip install ipython && ipython")
print("\nYour trading bot is NOT affected by this error!")
